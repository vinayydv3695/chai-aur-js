const name = "vinay";

const repo = 50;

//console.log(name + repo "Value")

console.log(" hello my   is ${repo}  and mine is ${name} ");

console.log("hello ${name}");

const namen = new String("vinay");

console.log(namen[0]);

console.log(namen.__proto__);
console.log(namen.length);
console.log(namen.toUpperCase());
console.log(namen.charAt(2));
console.log(namen.indexOf("y"));

const newString = namen.substring(0, 4);

const dusriString = namen.substring((2, 4));

console.log(dusriString);

const stringnew = "  vinay  ";
console.log(stringnew);
console.log(stringnew.trim());

const url = "https://google.com/cornhub%20.com";
url.replace("%20", "-");

console.log(url);

url.includes(cornhub);
url.split(google);
